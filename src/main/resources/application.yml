spring:
  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=msdb;trustServerCertificate=true
    username: sa
    password: Password12345!!
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServer2012Dialect